syntax = "proto3"; //[proto2|proto3]

option go_package = ".;pb";
package Game;

message raftLog {
    uint64 index = 1;
    uint64 term = 2;
    uint32 type = 3;
    bytes data = 4;
    int64 appendedAt = 5;
}